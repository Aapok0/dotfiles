{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    "schedule:weekly"
  ],
  "packageRules": [
    {
      "description": "Group all CI deps (Actions + workflow tool pins) into one PR",
      "matchFileNames": [".github/workflows/**"],
      "groupName": "ci dependencies"
    },
    {
      "description": "Automerge CI-validated updates (the group merges only if every member is automergeable)",
      "matchFileNames": [".github/workflows/**"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "automerge": true
    },
    {
      "description": "Installer pins run at 'just install' (not CI-tested); delta/zoxide/yazi/nerd-fonts also need a manual SHA256 refresh. One grouped PR, never automerge.",
      "matchFileNames": ["justfile"],
      "groupName": "tooling (justfile)",
      "automerge": false
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "CI tool versions pinned in workflow env: blocks (annotated with # renovate:)",
      "managerFilePatterns": ["/^\\.github/workflows/.+\\.ya?ml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)(?: versioning=(?<versioning>\\S+))?\\s+\\w+_VERSION:\\s*[\"']?(?<currentValue>[^\"'\\s]+)"
      ]
    },
    {
      "customType": "regex",
      "description": "Pinned tool versions in justfile (annotated with # renovate:)",
      "managerFilePatterns": ["/(^|/)justfile$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)(?: versioning=(?<versioning>\\S+))?\\s+\\w+_version\\s*:=\\s*\"(?<currentValue>[^\"]+)\""
      ]
    }
  ]
}
